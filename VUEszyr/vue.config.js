module.exports = {
    // baseUrl: process.env.NODE_ENV === 'production'
    //     ? '/production-sub-path/'
    //     : '/'
    // baseUrl:'VUEszyr/dist'
    //    修改打包出来的页面的baseUrl，在不放在根目录的时候有用
};
